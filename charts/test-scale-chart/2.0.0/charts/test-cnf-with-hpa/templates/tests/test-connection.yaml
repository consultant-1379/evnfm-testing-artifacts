apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "test-cnf-with-hpa.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "test-cnf-with-hpa.name" . }}
    helm.sh/chart: {{ include "test-cnf-with-hpa.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: {{.Values.global.registry.url}}/proj-am/busybox/busybox:latest
      command: ['wget']
      args:  ['{{ include "test-cnf-with-hpa.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
